zark (1.0.8-1) unstable; urgency=medium

  * Out-of-space handling: preventive guards plus reactive detection in
    backup and recover, replacing silent fall-through with clear fatals.
  * Backup: preventive guard before syncoid (refuses if target free
    space is below max(source.used / 100, 1 GiB)); reactive ENOSPC
    detection at the rpool, bpool incremental, and bpool from-scratch
    resync sites. The rpool check runs before the existing partial-
    success branch so an early ENOSPC no longer falls through into
    the bpool sync. bpool ENOSPC deliberately does not auto-recreate:
    destroying a stale bpool target when the drive is full would
    leave the user with no boot backup at all.
  * Recover: preventive disk-size check before the YES confirmation
    (compares target size from lsblk -bdn -o SIZE against rpool.used
    plus 5% overhead for ESP, bpool, swap and metadata); reactive
    ENOSPC detection at every send/receive site (raw_send, bpool
    send/receive, keystore final send) — every ENOSPC fatal because
    partial recovery is unbootable.
  * lib.sh.run_pipe() now reports a non-zero return code if either
    side of the pipeline failed. Previous implementation returned only
    p2.returncode, hiding p1 failures: a `zfs send` of a deleted
    snapshot followed by clean EOF on the receiver was reported as
    success and downstream code proceeded as if the transfer had
    worked. The fix waits explicitly for p1, captures both stderr
    streams, concatenates them, and picks the most informative
    non-zero return code (p2 takes precedence when both fail).
  * New helpers: lib.sh.is_enospc() with case-insensitive markers
    calibrated against real ZFS / kernel output ("no space left on
    device", "out of space", "enospc", "disk quota exceeded");
    lib.sh.humanize_bytes() for IEC-formatted sizes used in fatal
    messages; lib.zfs.dataset_used_bytes() and PoolInfo.size_bytes
    as numeric companions to the existing string fields.
  * backup: fresh snapshots before every replication. Backup now
    invokes `sanoid --take-snapshots` automatically before running
    syncoid (previously relied on the sanoid systemd timer, which
    meant interactive backups could replicate state up to ~1 hour
    old). Snapshotting is cheap (no I/O on the backup drive,
    idempotent within sanoid retention) so it's the default. The
    new --no-snapshot flag skips this stage for re-runs after a
    transient failure or for invocations that have triggered sanoid
    by other means. A failure of sanoid is non-fatal: backup
    proceeds with whatever snapshots already exist. Backup is now
    a 9-step process (was 8); the new step 6 sits between
    "Loading encryption key" and "Running syncoid".
  * simulate: GL acceleration auto-detect — when the host has both
    /dev/dri/renderD* and virtio-vga-gl in qemu-system-x86_64 -device
    help, uses -device virtio-vga-gl,edid=on -display gtk,gl=on for
    a resizable, hardware-accelerated GTK window. Falls back to
    -device virtio-vga,edid=on -display gtk with a warning naming
    the missing capability when either probe fails. New --display
    WxH flag overrides the default 2560x1440 EDID-advertised
    resolution; values above 8K are rejected. Documentation updated
    to reflect that read-only is the default and --rw is the
    explicit opt-in (the manpage and README previously documented
    a --ro flag that has long been a no-op for backwards
    compatibility).
  * Tests: 214 unit tests (up from 147), all passing, ruff clean,
    pylint 10.00/10. New coverage for run_pipe (5), is_enospc (7),
    humanize_bytes (8), dataset_used_bytes (3), backup preventive
    guard (8), backup --no-snapshot parsing (3), recover disk-size
    check (9), and simulate --display parsing (extended from 5
    to 12). New `make test-enospc-real` target running
    tests/test_enospc_real.sh — opt-in empirical calibration of
    the ENOSPC markers against the running kernel's actual ZFS
    error strings. Root-only, not invoked by `make fulltest`, not
    in CI; run after upgrading zfsutils-linux or moving to a new
    distro.

 -- Juanmi Taboada <juanmi@juanmitaboada.com>  Fri, 08 May 2026 14:35:00 +0200

zark (1.0.7-1) unstable; urgency=medium

  * Cross-host recovery, Secure Boot fixes, syncoid version
    compatibility.
  * Recover now pins shimx64.efi.signed and grubx64.efi.signed to .latest
    before dpkg-reconfigure, so a recovered system never inherits a
    revoked .signed.previous binary even when the source install (left
    by subiquity) happened to point at one. Fixes the "Verifying shim
    SBAT data failed: Security Policy Violation" failure mode reported
    after Ubuntu 26.04 fwupd SBAT updates.
  * Setup gains a Secure Boot pre-check (step 5) that inspects the live
    system's shim/grub alternatives and prompts (default NO) before
    switching them to .latest.
  * bpool features pruned to the GRUB-compatible subset documented in
    /usr/share/zfs/compatibility.d/grub2. Earlier zark versions activated
    head_errlog and vdev_zaps_v2 on bpool, which GRUB 2.14 (Ubuntu 26.04)
    cannot read — boot failed with "file not found" and "prohibited by
    secure boot policy" for every kernel. Existing recoveries hit by
    this need a fresh `zark recover` with this version.
  * fix_grub_bpool_uuid now rewrites every --fs-uuid line in grub.cfg,
    including those carrying --hint-bios / --hint-efi / --hint-baremetal
    options between --set=<name> and the UUID. The previous regex
    silently skipped these lines, leaving the source machine's stale
    bpool UUID in the if-branch of every menu entry on grub 2.12+. The
    bug stayed hidden whenever the recovered disk landed at the same
    BIOS index as the original, but produced "no such device: <old-uuid>"
    on cross-host recovery (different machine, different drive
    enumeration). Two new unit tests cover the regex.
  * New lib.zfs.syncoid_exclude_flag() helper auto-selects --exclude
    (sanoid 2.1.0 - 2.2.0-2, Ubuntu 22.04 through 25.10) or
    --exclude-datasets (sanoid 2.3.0+, Ubuntu 26.04+). Earlier versions
    hard-coded --exclude-datasets and silently produced a backup with
    keystore + bpool but no rpool on older sanoid.
  * New, source retention now follows sanoid policy alone; backup
    auto-resolves divergence under 64 MB silently and aborts with a
    pointer to interactive `zark repair-divergent` for anything larger.
  * Repair command split into repair-boot (fix grub.cfg / initrd from
    a live USB) and repair-divergent (interactively reset diverged
    backup datasets). Total command count is now 14.
  * bpool/BOOT snapshot policy moved from sanoid template_minimal
    (2 days) to template_production (~3 months) so the boot chain has
    the same rollback horizon as user data.

 -- Juanmi Taboada <juanmi@juanmitaboada.com>  Wed, 06 May 2026 22:50:31 +0200

zark (1.0.6-1) unstable; urgency=medium

  * Add zark(1) manpage. Source kept as Markdown in debian/zark.1.md;
    converted to roff at build time via pandoc + sed + awk in
    debian/rules. The pipeline normalises pandoc 3.x's bracket-form
    font selectors and drops redundant .PP after section macros so the
    output passes both `mandoc -Tlint` and `groff -ww -z` cleanly.
    Build-Depends adjusted accordingly.
  * Stop dpkg-source choking on tool caches in the working tree. Add
    debian/source/options with tar-ignore + extend-diff-ignore for
    .mypy_cache, .pytest_cache, .ruff_cache, .tox, __pycache__, *.pyc,
    .DS_Store and zark.log so a build never aborts with "unexpected
    upstream changes" after running mypy/pytest/ruff locally.
  * Stop bundling debian/ inside the .orig.tar.gz. The 3.0 (quilt)
    format expects a clean upstream slice; including debian/ produced
    a redundant overlay (lintian W: no-debian-changes) and broke the
    Debian-revision-bump workflow. The Makefile's `make dist` target
    now strips debian/ when assembling the upstream tarball.
  * Make lib/config.py the single source of truth for the version.
    The manpage source carries @VERSION@ in its header, substituted
    by both `make manpage` and debian/rules before pandoc runs (post-
    pandoc substitution would have to match the \[at] escape pandoc
    --ascii emits, which is fragile). The bug-report issue template
    placeholder and the example versions in debian/README.packaging.md
    were moved to a generic X.Y.Z form. Releasing now means editing
    lib/config.py and the two changelogs only.

 -- Juanmi Taboada <juanmi@juanmitaboada.com>  Sun, 03 May 2026 12:00:00 +0200

zark (1.0.5-1) unstable; urgency=medium

  * Initial Debian/Ubuntu packaging.
  * Distribution mechanism for zark on Ubuntu systems where the tool is
    used for day-to-day backups. The portable tarball workflow is
    preserved unchanged for live-USB disaster recovery — the .deb
    package targets the "install on the productive host" use case only.
  * Installs sources under /usr/share/zark/, exposes the entry point as
    /usr/bin/zark via symlink, and ships an example known_drives.json
    in /etc/zark/. The configuration directory search order in
    lib/config.py (env override -> portable etc/ -> /etc/zark/) makes
    this layout work without code changes.
  * Depends: zfsutils-linux, sanoid, cryptsetup-bin, gdisk, grub2-common
    and one of dracut | initramfs-tools — every external command zark
    invokes outside the recover-only path. Recommends pulls in the
    recover dependencies (dosfstools, grub-efi-amd64-signed, shim-signed)
    and the simulate dependencies (qemu-system-x86, ovmf).

 -- Juanmi Taboada <juanmi@juanmitaboada.com>  Fri, 01 May 2026 12:00:00 +0200
